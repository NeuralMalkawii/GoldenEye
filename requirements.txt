# Runtime dependencies for the API image (mirrors pyproject [project].dependencies).
# The app runs as `src.api.main:app` from the repo root, so the local package is
# NOT pip-installed — only these third-party deps are.
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2
python-multipart
aiofiles
onnxruntime>=1.18
opencv-python-headless>=4.10
numpy<2
celery[redis]>=5.4
redis>=5
structlog>=24
slowapi>=0.1.9
prometheus-fastapi-instrumentator>=7
websockets>=12
python-dotenv
pydantic-settings>=2
sentry-sdk[fastapi]>=2
